# -*- coding: utf-8 -*-
"""你的求职方向 —— 想加岗位方向，只改这个文件就够了。

这里是与引擎隔离的「用户区」，你能安全折腾：
  • 加错了不影响抓取——引擎每天抓的是全量岗位，方向只决定「从里面挑哪些给你看」；
  • 写错语法也不会让每日任务崩——引擎会忽略你的自定义、回退到内置方向，并在日志里提示；
  • 更新上游代码时，你的方向都在这个独立文件里，不容易冲突。

怎么加：在 MY_DIRECTIONS 里加一行 "方向名": ["关键词1", "关键词2", ...]
  • "方向名" 会显示在日报的关注方向里；
  • 关键词用来匹配岗位标题——岗位标题里含任一关键词，就算命中这个方向；
  • 和内置方向同名会「合并」关键词、不会覆盖（放心加）。

改完本地验证一下（可选）：
    PYTHONUTF8=1 python -m pytest tests/test_user_profile.py -q
"""

MY_DIRECTIONS = {
    # 把下面几行的 # 去掉就能启用，或照着格式加你自己的方向：

     "法务合规": ["法务", "合规", "知识产权", "风控", "政策研究"],
    "金融投资": ["投资经理", "行业研究", "募资", "投后管理", "财务分析"],
    "翻译语言": ["翻译", "本地化", "口译", "笔译", "字幕"],
}
